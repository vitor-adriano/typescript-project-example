{
  "name": "@tpe/server",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.10.7"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.8",
    "@types/mongoose": "^5.7.36",
    "ts-node-dev": "^1.0.0-pre.63",
    "tsconfig-paths": "^3.9.0"
  },
  "scripts": {
    "build": "babel src --extensions \".js,.ts\" --out-dir dist --copy-files --no-copy-ignored",
    "dev": "tsnd -r tsconfig-paths/register --cls --respawn --transpile-only --ignore-watch node_modules src/index.ts"
  }
}
